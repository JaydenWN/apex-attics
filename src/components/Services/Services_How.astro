---
import OneIcon from "@icons/one-orange.svg";
import TwoIcon from "@icons/two-orange.svg";
import ThreeIcon from "@icons/three-orange.svg";
---

<section>
    <div class="container">
        <h2>How Our Attic Conversion Process Works</h2>
        <p>
            We’ve made it easy to upgrade your home with safe and functional
            attic storage. From your first quote to final clean-up, we handle
            everything.
        </p>

        <div class="how-list">
            <div class="how-card">
                <div class="card-head">
                    <OneIcon class="icon" />
                    <h3>Request a Free Quote</h3>
                </div>
                <p>
                    Send us an enquiry or give us a call. We’ll ask a few simple
                    questions about your home and provide an initial estimate.
                </p>
            </div>

            <div class="how-card">
                <div class="card-head">
                    <TwoIcon class="icon" />
                    <h3>On-Site Assessment</h3>
                </div>
                <p>
                    We visit your property to assess your attic space, confirm
                    suitability and answer any questions. This inspection is
                    100% free and comes with no obligation.
                </p>
            </div>

            <div class="how-card">
                <div class="card-head">
                    <ThreeIcon class="icon" />
                    <h3>Same-Day Installation Available</h3>
                </div>
                <p>
                    Happy to go ahead? In many cases, we can complete the full
                    attic storage conversion on the same day as your site visit.
                </p>
            </div>
        </div>
    </div>
</section>

<style>
    .container {
        display: flex;
        flex-direction: column;
        gap: 2rem;
    }
    .how-list {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
    .how-card {
        border-radius: 8px;
        background-color: #f6faf7;
        box-shadow: var(--box-shadow);
        display: flex;
        flex-direction: column;
        gap: 1rem;
        padding: 1rem;
    }
    .card-head {
        display: flex;
        align-items: end;
        gap: 1rem;
    }
    .container > h2,
    .container > p {
        text-align: center;
    }

    @media (min-width: 768px) {
        .container {
            gap: 4rem;
        }
        .how-card {
            padding: 2rem;
            gap: 2rem;
        }
    }

    @media (min-width: 1024px) {
        .how-list {
            display: grid;
            grid-template-columns: 1fr 1fr;
        }
        .card {
            box-sizing: border-box;
        }
        .how-card:last-of-type {
            grid-area: 2 / 1 / 3 / 3;
        }
        .icon {
            min-width: 50px;
            height: 50px;
        }
    }

    @media (min-width: 1440px) {
        .container {
            gap: 4rem;
        }
        .container > p {
            width: 560px;
            margin: 0px auto;
        }
        .how-list {
            grid-template-columns: 1fr;
            gap: 2rem;
            margin-top: 4rem;
        }
        .how-card:last-of-type {
            grid-area: auto;
            margin-left: 8rem;
        }
        .how-card:nth-of-type(2) {
            margin-left: 4rem;
        }
    }
</style>
